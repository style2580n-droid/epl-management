# 인수인계 (2026-07-22 A단계 완료 기준) — epl-management 축구 데이터 파이프라인

## 📌 새 채팅 시작 안내 (여기부터 읽으세요)

**이 프로젝트는 뭔가:** GitHub `epl-management` 저장소의 축구 데이터 파이프라인.
매일 자동으로 7개 리그(EPL + 6개 리그) 데이터를 수집해 두 개의 예측 앱에 공급한다.
지금은 개막(8월 말) 직전이라 "만드는 단계"는 끝났고 "지켜보는 단계"다.

**연속성(누락 걱정 해소용):** 이 문서는 07-17~18 원본 인수인계 → 이후 세션들의
작업까지 전부 이어받아 통합한 것이다. 원본에서 미완이던 6개 항목은 지금
**완료 3 / 보류 2(사용자 결정) / 개막대기 1**로 전부 처리·분류됐고 증발한 항목 없음.
(상세는 아래 "✅ 검증 완료" 및 "⏸ 보류" 참조.)

**저장소에 반영해야 할 파일 (A단계 결과물 — 아직 안 올렸으면 올릴 것):**
- `app_export_multileague.py` (6개 리그 데이터 연결)
- `app_export.py` (EPL 데이터 연결)
- `EPL_index.html` (EPL 클라이언트 코드)
- `multi_league_index.html` (6개 리그 클라이언트 — 별도 보조 세션이 만든 것. 저장소에 반영됐는지만 확인)
- `rehearse_lineups_probe.py` (**수정본**: 재귀 전수탐색 방식. 1차본은 버그로 오판했음 — 아래 "다음 할 일 0번" 참조)
- `football_pipeline.yml` (프로브 실행줄 포함된 버전. 라인업 검증 끝나면 그 줄 제거)
- ⚠️ **`collect_lineups.py` 작성 시 `collect_goalscorers.py` 최신본이 필요하다**
  (템플릿용). 세션 간에 돌아다니는 사본은 옛 버전일 수 있으니 **반드시 저장소에서
  받은 것**을 쓸 것 — 최신본엔 `_CONFIRMED_TPL`(incidents 확정), 증분 캐싱(`checked`/
  `nil`), `_load_prev()`가 들어 있다. 이게 없으면 옛 버전이다.
- (앞서 반영 완료분: collect_*.py, worldcup2026.json, player_baseline.json)

**새 세션은 저장소 파일에 직접 접근 못 함**(네트워크 off, GitHub 커넥터 없음).
파일 확인·수정이 필요하면 사용자가 해당 파일을 업로드해야 한다. 필요할 때 콕 집어 요청할 것.

**다음 실작업:** 0번(라인업→apps 확장)·0-B(baseline 갱신 경로 버그 3건) 둘 다
2026-07-23에 수정+테스트 완료. **저장소에 반영하고 다음 실전 파이프라인 로그로
최종 확인만 하면 됨** — 확인할 로그: `[collect_lineups] epl: ...`,
`[app_export_multileague] 선수 기준선 갱신: ... 팀불일치로 스킵 N건, 지난시즌
(EPL) 제외 N건`. 그 다음은 개막(8월 말) 대기 — 새로 팔 것 없음.

---

> **새 세션 필독 규칙**
> 1. 아래 "✅ 검증 완료"는 실전 파이프라인 로그로 이미 증명된 것들이다.
>    **재검증하지 말고, 로그 재요청하지 말고, 다시 묻지 말 것.** 이 문서가 검증 기록이다.
> 2. 대화는 짧게. 배경 설명 늘어놓지 말 것. 필요한 것만 정확히 요청할 것.
> 3. 코드 수정 시 함수 단위 테스트로 끝내지 말고 **`main()` 전체를 실제로 돌려서**
>    스코프/연결부까지 확인할 것. (과거에 이걸 안 해서 파이프라인을 한 번 죽였음.)
> 4. "추측 금지" 원칙: 스키마/필드/표기는 실행 로그로 실측 확인한 뒤 확정한다.
> 5. 사용자가 헛소리/재확인 반복에 예민함. 이미 검증된 걸 다시 의심하지 말 것.

---

## 시스템 개요
- GitHub 저장소 `epl-management`, Actions 워크플로우 "Football Data Center Pipeline"의
  report 잡에서 수집기들을 순차 실행. 정상 완주 시 약 5분.
- 앱 2개(아티팩트): EPL 전용 `EPL_index.html`, 6개 리그 `multi_league_index.html`.
- 6개 리그 = laliga, bundesliga, seriea, ligue1, eredivisie, championship (EPL은 별도 앱).
- 메인 데이터 소스: BSD(BSDClient, api_clients.py). 보조: football-data.org, TheSportsDB, FPL.
- 출력: `reports/app_data.js`(EPL), `reports/app_data_multileague.js`(6개 리그).

---

## ✅ 검증 완료 — 재확인 금지 (괄호 안은 증거)

1. **팀 매칭 7개 리그 전부 완성** (#74 로그)
   - EPL 20/20, 챔피언십 **24/24**, 라리가 클럽 20/20, 리그앙 18/18, 분데스 18, 세리에A 20, 에레디비시 18.
   - 실측 확정된 BSD 표기: 울버햄튼=`Wolverhampton`(단독, team_id=11), 데포르티보=`Deportivo de A Coruña`, 라싱=`Real Racing Club`, 브레스트=`Stade Brestois`.

2. **라리가 BSD 중복 레코드 4쌍** — 버그 아님, 정상
   - 아틀레틱 911/51, 아틀레티코 54/920, 에스파뇰 53/919, 레알소시에다드 48/924.
   - 대표 ID 자동 선택으로 처리(diag 정상 출력). "팀 24개, 클럽 20개"는 이 중복 때문이며 클럽 수가 20이면 정상.

3. **가짜 이적 근절** (#74) — 원인=위 중복 ID. collect_transfers_bsd에 대표 ID + 같은클럽 가드 적용, app_export 표시단 필터 + 누적 json 청소. "레소시→레소시" 류 재발 없음. 진짜 이적만 정상 감지(우고 소텔로, 디에고 콘데 등). 누적 253건.

4. **득점왕 엔드포인트 실측 확정 = `events/{eid}/incidents/`**
   - EPL 지난시즌 722경기 중 251건 득점자 추출 성공(#74). 증분 재사용 722건.
   - 리그 단위 topscorers/top_scorers/scorers는 전부 404 = BSD에 없음 확정.
   - event_detail에는 득점자 필드 없음(점수만) 확정. → incidents가 유일한 경로.

5. **득점왕 배관 실시간 검증 완료** (rehearse #74) ★ 오늘 핵심
   - UEFA 예선 등 실시간 종료경기로 리허설: 대회 6개, 종료 2843건, 프로브 48건, **골 확보 46건 → ✅**.
   - 득점자·도움·분까지 정확 파싱(레반도프스키 63', 요케레스 3골, 케인 80' 등).
   - **결론: 8월 개막 시 득점왕 리더보드가 실전에서 채워짐이 증명됨.** 더 기다릴 것 없음.

6. **월드컵 여독(fatigue) 병합 완료** (#74) ★ 오늘
   - `data/master/worldcup2026.json`(정적) + `build_worldcup()`. 로그: "월드컵: fatigue 47팀(감쇠 1.00), 부상 병합 1건".
   - 선수 152명 출전기록 → 팀별 0~1 fatigueScore. 결승(7/19) 후 14일 100%, 56일차 0%로 선형 감쇠. 개막 무렵 약 69%.
   - 앱은 fatigueScore를 받아 예측에 최대 -10% 반영(이미 연결됨).
   - 부상: 시트 부상 중 6개 리그 해당자 사이바리(PSV)만 병합, 8/31까지 유효. 사유 미상 결장 4명은 추측 금지로 json에 기록만(merge:false → 뉴스 확인 시 true로).

7. **선수 능력치 기준선 (C단계) 완료** (#74) ★ 오늘
   - `data/master/player_baseline.json` + `update_player_baseline()`. 선수 888명.
   - per90 지표: scoring=(골+xG)/90, creating=(도움+xA)/90, defending=(인터셉트+태클)/90.
   - 벨링엄 180분 8골류 왜곡은 라운드 보정(82명)으로 해결.
   - 시즌 중 goalscorers 리그 기록이 누적되어 league_apps≥5면 source를 wc2026→league로 자동 전환.
   - 이름 매칭: 월드컵=성만(오야르사발) vs 리그=풀네임(미켈 오야르사발) 형식차 → 성(마지막 토큰) 유일할 때만 폴백 매칭.
   - ⚠️ 지금은 리그 경기 0건이라 "선수 기준선 갱신" 로그가 안 나오는 게 정상(개막 후 나옴).

8. 감독 수집 20/20·22/22, players `position` 필드 존재, managers 캐시 ~2900명, 로고 109건, 스쿼드 포지션 118팀, H2H, xG 등 전부 이전 세션+로그로 검증 완료.

9. **파이프라인 정상 완주** (#74, 5분) — 지난번 wc_fatigue NameError로 죽었던 것 해결됨. 요약 루프도 리그 통째 실패 시 안 죽게 `.get()` 방어 완료.

10. **A단계 = 선수 능력치를 예측 엔진에 반영 — 클라이언트·서버 코드 전부 완료 (EPL·6개 리그 둘 다)** (2026-07-22~23)
    - **작동 원리**: player_baseline.json의 선수 중 `source==="league"`(리그 실기록으로 갱신된 선수)만 club_en으로 팀 매칭 → 팀별 (scoring+creating) per90 평균이 리그평균 대비 높/낮으면 공격 λ를 ±8% 내에서 상/하향. 팀당 매칭 선수 4명(PLAYER_MIN_SAMPLE) 미만이면 무효과(폴백).
    - **6개 리그**(multi_league_index.html): 클라이언트 코드 완성 + 데이터 경로 정상. 개막 후 자동 활성화됨.
    - **EPL**(EPL_index.html): 클라이언트 코드 완성(PLAYER_BASELINE, teamMatchedBaseline, playerBaselineLambdaAdjust, predictMatch 연결, PIPELINE_PLAYER_BASELINE 병합) + **2026-07-23 baseline 갱신 경로 버그 3건 수정 완료(아래 0-B)** — EPL도 이제 개막 후 자동 활성화됨.
    - 검증: 파이썬 문법 OK, source=league 필터 실동작, EPL HTML 괄호균형(5733/5733)·babel 파싱 OK. EPL 데이터 경로 버그(0-B)는 코드 레벨 검증(main() 실행)으로 확인 완료 — 실전 로그 확인은 개막 후 첫 확인점.

---

## 🔬 검증 방법 상세 (어떻게 검증했나 — 새 세션이 신뢰할 근거)

각 항목이 "말로만 완료"가 아니라 실제로 어떻게 확인됐는지 기록. 이 방식으로 이미
검증된 것이므로 새 세션은 재검증 불필요.

**실전 파이프라인 로그로 검증 (GitHub Actions report 잡 실행 = #74 등):**
- 팀 매칭 7개 리그: 각 리그 `teams{...} → 응답 N개 중 M개 ID 매칭(클럽 X개, 기대 Y팀)` 로그로 클럽 수 확인. 스크린샷으로 대조.
- 라리가 중복 4쌍: `[diag] laliga "레알 소시에다드" 중복 2건 [48:.., 924:..] → 대표 id=48` 로그로 실제 ID·표기 확인.
- 가짜 이적 근절: 로그에 "레소시→레소시" 사라지고 진짜 이적만(우고 소텔로 등). 누적 이적 253건.
- 득점왕 251건: `[collect_goalscorers] epl: 종료경기 722건 중 득점정보 확보 251건`.
- 득점왕 실시간 리허설: `[rehearse_uefa] ✅ ... 골 확보된 경기 46건` + 실제 득점자 이름·분 로그(레반도프스키 63' 등).
- 월드컵 fatigue: `[app_export_multileague] 월드컵: fatigue 47팀(감쇠 1.00), 부상 병합 1건`.
- 파이프라인 완주: report 잡 succeeded, 약 5분. 로그 끝까지 정상 출력.
- 감독/포지션/로고: `players position 필드 존재`, `managers 2900명`, `로고 캐시 재사용 109건` 로그 + 스크린샷.

**코드 레벨 검증 (이 환경에서 실제 실행 — A단계/월드컵/기준선):**
- 파이썬: `py_compile` 문법 검사 통과 (app_export.py, app_export_multileague.py, collect_*.py).
- `main()` 전체 실행: 스텁 클라이언트로 main을 끝까지 돌려 스코프/연결부 확인(과거 wc_fatigue NameError를 이 방식으로 잡음).
- 월드컵 병합: build_worldcup을 7/20·8/15·9/14 세 시점으로 돌려 감쇠(1.0→0.69→0)·부상 유효기간·무파일 폴백 9개 체크 통과.
- 기준선 갱신 훅: 모킹 goalscorers로 오야르사발 5경기 → source=league 전환 확인.
- A단계 필터: source=league 필터가 개막 전 0명 → 오야르사발 전환 시 1명(club_en=Real Sociedad, per90 포함) 정확 포착.
- 리허설 스크립트: 모킹으로 대회발견/국내리그제외/골파싱/자책골제외/성공판정 검증 후 실전 투입.

**HTML(클라이언트) 검증:**
- EPL: 추가 함수·변수 9개 존재, playerBaselineLambdaAdjust 괄호 균형, predictMatch 체인 순서(부상→선수능력치), 전체 중괄호 균형 유지(5733/5733), babel script 블록 파싱 OK.
- 6개 리그: 별도 보조 세션이 작성, 사용자가 업로드본으로 확인(playerLambdaAdjust 등 존재 확인).

**한계로 남긴 것(정직하게):** A단계 defending(수비) per90은 월드컵 값 고정 — 라인업 데이터 없어 시즌 갱신 불가. 득점왕/기준선의 "리그 실데이터" 부분은 개막 전이라 실전 데이터로는 미검증(모킹·과거시즌으로만 검증). 개막 후 첫 경기가 최종 확인점.

---

## 저장소 반영된 최신 코드 (전부 반영 완료 — 다시 주지 말 것)
- `collect_fixtures_multileague.py`: teams 페이지네이션, 중복/미매칭/unknown id/스쿼드0명 diag, PRIMARY_TEAM_IDS(클럽당 대표 ID).
- `app_export_multileague.py`: 별칭 4건, 가짜이적 필터, build_worldcup(fatigue+부상), update_player_baseline(C단계 훅), 요약루프 .get() 방어, render_js에 wc_fatigue 파라미터, **+A단계: 리그블록에 playerBaseline(source=league 필터) 추가**.
- `app_export.py`(EPL용): **+A단계: PIPELINE_PLAYER_BASELINE const 추가(source=league 필터)**.
- `EPL_index.html`: **+A단계: PLAYER_BASELINE/playerBaselineLambdaAdjust + predictMatch 연결 + 로더 병합**.
- `multi_league_index.html`: A단계 클라이언트 코드 이미 완성(이전 세션). 이번 세션 미변경.
- `collect_transfers_bsd.py`: 대표 ID + 같은클럽 가드 + 누적 가짜 청소.
- `collect_goalscorers.py`: incidents 확정경로 상시 사용, 증분(골/0-0/checked 스킵), 경로소실 시에만 비상 프로브.
- `data/master/worldcup2026.json`, `data/master/player_baseline.json`: 정적 데이터(반영됨).
- `football_pipeline.yml`: **정리본으로 교체 완료 = 리허설 줄 제거된 상태**여야 함(2026-07-22 정리). 만약 저장소에 아직 `python rehearse_goalscorers_uefa.py || true` 줄이 남아있으면 그 줄 + 위 주석 3줄 삭제할 것.
- `rehearse_goalscorers_uefa.py`: 리허설 전용. yml에서 호출만 빼면 잠자는 상태. 남겨둬도/지워도 무방. 재검증 필요 시 yml에 `python rehearse_goalscorers_uefa.py || true` 한 줄만 다시 넣으면 됨.

---

## ▶ 다음 할 일 (개막 후 자동 진행 위주)

0. **🔥 진행 중 (이어서 바로 할 것): BSD 라인업 → league_apps 확장**

   **실측 확정 (2026-07-23 프로브 재실행 로그, 재조사 불필요):**
   - ✅ **경로 = `events/{eid}/lineups/`**. 내부 구조도 확정:
     `lineups.home.players`(선발 11명), `lineups.home.substitutes`(벤치 9명), away도 동일.
     `unavailable_players`(결장자), `lineup_status`, `updated_at` 키 존재.
   - ✅ **선수 항목 필드(실측)**: `['ai_score', 'id', 'jersey_number', 'name',
     'position', 'short_name']`
   - ❌ 나머지 경로 전부 404: `/lineup/`, `/formations/`, `/players/`, `/squads/` → 재시도 금지

   **⚠️ 중요 정정 — 이전 판단이 틀렸음:**
   이전 인수인계에 "라인업 수집기를 붙이면 defending per90도 갱신 가능"이라고
   적혀 있었으나 **틀렸다.** 실측 결과 라인업 응답에는 `minutes_played`,
   `is_starter` 같은 출전시간 필드도 없고, 태클/인터셉트 같은 **수비 이벤트
   카운트가 아예 없다**(포지션·명단 정보뿐). 따라서:
   - ✅ **가능**: 선발 11명을 출전으로 카운트 → **무득점 선수도 league_apps 누적**
     (현재는 득점 관여 선수만 카운트되는 한계 해소). 이게 실질 소득.
   - ❌ **불가능**: `defending` per90 실측 갱신. 수비 이벤트 소스가 BSD에 없음.
     → **별도 미해결 항목으로 분리**(아래 5번). 라인업으로는 해결 안 됨.
   - `ai_score` 필드가 있으나 용도 불명 → **추측으로 쓰지 말 것.** 필요하면 별도 실측.
   - 벤치(`substitutes`) 중 실제 교체 투입된 선수는 이 응답으로 구분 불가.
     → league_apps는 **선발 11명만** 카운트하는 게 안전(과대집계 방지).

   **다음 단계(순서)**:
   (1) `collect_lineups.py` 작성 — 기존 `collect_goalscorers.py`를 템플릿으로
       (증분 캐싱/리그별 순회/저장 포맷 일관성). **⚠️ 이 세션에 있는
       collect_goalscorers.py는 옛 버전이므로 저장소에서 최신본을 받아 쓸 것.**
       출력: `data/master/lineups.json`
   (2) `update_player_baseline()`에 연결 → 선발 출전 기반 league_apps 누적
   (3) 검증 후 yml에서 프로브 줄(`python rehearse_lineups_probe.py || true`) 제거
   (4) (선택) `unavailable_players`로 결장자 정보 확보 가능한지 확인 —
       현재 부상 소스(API-Football)가 개막 전 0건이라 보강 가치 있음

0-B. **✅ [버그 3건, 2026-07-23 전수 감사 → 같은 날 수정+테스트 완료] baseline 갱신 경로 결함**

   ※ 재검증 불필요 — 아래 "검증 방법"으로 이미 확인됨. 다음 실전 파이프라인
   실행 로그로 최종 확인만 남음(코드 레벨 검증은 끝).

   **(1) EPL이 baseline 갱신 루프에서 빠져 있던 것 → 수정 완료**
   - 수정: `update_player_baseline()`에서 `_BASELINE_LEAGUE_KEYS =
     list(LEAGUE_TEAM_MAPS) + ['epl']`로 EPL도 포함. LEAGUE_TEAM_MAPS 자체는
     안 건드림(다른 함수가 6개 리그 전용으로 쓰고 있어서).
   - **season_id 필터**: goalscorers.json/lineups.json의 EPL 경기 레코드엔
     season_id가 없음(collect_fixtures.py 이벤트 응답에 있는지 미확인 상태로
     남김 — 대신 이미 있는 `date` 필드로 필터). `_EPL_SEASON_START =
     '2026-07-01'`(실측 확정된 26-27 시즌 시작일) 이전 경기는 apps 집계에서
     제외(`_in_current_epl_season()`). 6개 리그는 원래도 현재 시즌만 잡혀서
     영향 없음.

   **(2) 이름 매칭에 소속팀 검증이 없던 것 → 수정 완료**
   - 수정: `_team_mismatch()` 추가 — 득점 레코드의 `team`(득점왕 루프) /
     LEAGUE_TEAM_MAPS 별칭(라인업 루프, 6개 리그만)과 선수 `club_en`을 관대한
     토큰 비교(FC/City 등 접미사 제거 후 겹치는 단어 있는지)로 대조. 명백히
     다른 팀이면 스킵, 정보 부족 시엔 기존처럼 통과(악화 없음).
   - **남은 한계(정직하게)**: EPL 라인업 쪽은 이 파일에 EPL 팀 영문 별칭
     소스가 없어서 팀검증 보류 — 이름(성 유일성)만으로 매칭됨. 리스크는
     상대적으로 낮음(입력이 골 귀속이 아니라 단순 출전 카운트라 오귀속 영향이
     apps 1건 정도로 작음). 필요하면 추후 별도 세션에서 EPL 팀 별칭 소스
     확보 후 보강.

   **(3) EPL 수출이 baseline 갱신보다 먼저 실행되던 것(stale) → 수정 완료**
   - 수정: `app_export.py`가 `app_export_multileague.py`에서
     `update_player_baseline`을 import해서 `render_js()` 안, baseline을 읽기
     **직전**에 직접 호출. 같은 실행에서 두 번(app_export.py용 1회 +
     app_export_multileague.py 자체 실행 1회) 불려도 매번 파일을 처음부터
     다시 계산해서 덮어쓰는 구조라 **멱등** — 순서 안 바꿔도 안전.
   - yml 자체는 안 건드림(app_export.py → app_export_multileague.py 순서 유지).

   **검증 방법 (코드 레벨, 이 환경에서 실제 실행 — main() 레벨, 함수 단위 아님)**:
   - 시즌필터: EPL 경기 2건(2025-08-10 지난시즌 / 2026-07-15 신시즌) 투입 →
     지난시즌 apps=0(제외), 신시즌 apps=1(반영) + 로그 "지난시즌(EPL) 제외
     1건" 확인.
   - 팀불일치 차단(득점왕): 이름은 유일하게 매칭되지만 실제 골은 다른 팀
     (Atlético Madrid) 소속인 케이스 투입 → 해당 선수(Everton 소속으로
     baseline 등록) apps=0 유지(오귀속 안 됨) + "팀불일치로 스킵 1건" 확인.
   - 팀불일치 차단(라인업): 알라베스 홈경기 선발 명단에 실제 알라베스 소속
     선수 1명 + Real Madrid 소속으로 등록된 동명이인 1명 투입 → 알라베스
     선수만 apps=1, Real Madrid 선수는 apps=0(스킵) 확인.
   - 멱등성: `update_player_baseline()`을 같은 프로세스에서 연속 2회 호출 →
     결과 JSON 바이트 단위 동일 확인(app_export.py+app_export_multileague.py
     이중 호출 시나리오 시뮬레이션).
   - 기존 회귀: 6개 리그 정상 케이스, 무득점 선수 라인업 5경기→source=league
     전환, lineups.json 없을 때 폴백 — 전부 기존과 동일하게 재확인.
   - `py_compile` 통과(app_export.py, app_export_multileague.py).

   **전수 감사에서 정상 확인된 것(재점검 불필요)**: main() 실행순서(갱신 925 → 수출 927),
   baseline 파일경로 양쪽 일치(`data/master/player_baseline.json`), EPL 선언/런타임 병합
   순서(TEAM_NAME_EN 1656 → 함수 3014~ → 로더 8745), 6개 리그 클라이언트 안전장치
   (source=league 필터 + PLAYER_MIN_SAMPLE 4 가드).

1. **A단계 효과 검증 & 확장** (클라이언트 코드는 완료 — 위 검증완료 10번 참조)
   - 개막 후 팀당 source=league 선수가 4명↑ 쌓이면 예측에 자동 반영됨. 그때 실제 예측이 합리적으로 움직이는지(강팀 λ 상향 등) 로그/앱에서 확인.
   - 보정 강도(현재 ±8%, rel×0.16)가 적절한지 실데이터로 튜닝 여지. 지금은 보수적으로 시작한 값.
   - **한계**: baseline 자동 갱신은 공격 지표(scoring/creating)만 쌓임. defending은
     월드컵 값 고정 — 라인업으로도 해결 안 됨이 실측 확정(위 0번·아래 5번 참조).

2. **개막(8월 말) 후 첫 리그 종료경기 확인** — `[collect_goalscorers] {리그}: 확보 N건`이 6개 리그에서 채워지는지 + "선수 기준선 갱신" 로그 등장 확인. (배관은 이미 리허설로 검증됨 → 순조 예상.)

3. **부상(훈련) 데이터** — 개막 임박 시 API-Football(injuries_af.json) 실데이터 재확인. 월드컵 부상 병합은 8/31 후 실데이터가 대체.

4. **ML 앙상블** — 26-27 경기 150건 쌓이면 train_ml_ensemble 자동 학습 시작(현재 표본 0건 스킵은 정상). 학습 후 EPL 하드코딩 트리 교체 실효성 비교.

5. **[미해결] `defending` per90 실측 갱신 — 소스 없음**
   - 상태: baseline의 defending은 **월드컵 값에 영구 고정**. 시즌 데이터로 갱신 불가.
   - 이유(실측): BSD `events/{id}/lineups/`엔 명단·포지션만 있고 태클/인터셉트 등
     수비 이벤트 카운트가 없음. `events/{id}/incidents/`는 골 이벤트 중심.
     → **BSD 안에는 수비 스탯 소스가 없는 것으로 보임**(단정은 아님, 추가 실측 여지).
   - 선택지: (a) 그대로 두기 — A단계는 공격 지표(scoring/creating)만으로도 작동 중,
     (b) 다른 소스 조사(SportScore는 RAPIDAPI_KEY 필요, FBref류는 별건),
     (c) BSD `players/`의 `attributes`/`rating` 필드가 수비 지표를 담는지 실측.
   - **사용자 판단 필요. 급하지 않음** — 개막 후 A단계 효과를 보고 결정해도 됨.

---

## ⏸ 보류 (사용자 결정 — 먼저 꺼내지 말 것)
- **odds-api.io 키가 `multi_league_index.html`에 평문 노출** → 서버 프록시 이전 필요하지만 사용자가 "패스" 결정. 사용자가 먼저 꺼내기 전엔 언급 금지.
- **순수 인명 오역**("양고기 도나트"류) — 사람이름 전용 음역 시스템 필요. 근본 미해결로 보류. (현재 구조적 오역 필터 _looks_like_mistranslation로 문장형 오역은 잡고 있음.)

---

## 🚫 하지 말 것
- 팀 매칭/별칭/diag 재점검 요청, sample_keys 재확인 요청.
- event_detail에서 득점자 찾기(없음 확정), 득점왕 엔드포인트 재프로브 설계(incidents 확정).
- 득점왕 실시간 작동 여부 재검증(리허설로 ✅ 증명 끝).
- 라리가 "팀 24개"를 문제로 취급(클럽 20개면 정상).
- "선수 기준선 갱신" 로그 없음을 버그로 취급(개막 전 정상).
- "ML 학습 스킵"을 문제로 취급(시즌 전 정상).
- BSD 502 에러를 코드 버그로 취급(서버 장애, 다음 실행 자동 복구. 스냅샷 보존 설계로 데이터 손실 없음).
- A단계 재구현/재설계(EPL·6개 리그 모두 코드 완료). "playerBaseline이 비어있다/0명이다"를 버그로 취급하지 말 것 — 개막 전엔 source=league가 0명이라 정상 휴면. 개막 후 자동 활성화됨.
- 라인업 경로 재탐색: `events/{eid}/lineups/`가 정답으로 실측 확정됨. `/lineup/`, `/formations/`, `/players/`, `/squads/`는 전부 404 확인 → 다시 프로브하지 말 것.
- 1차 프로브의 "❌ BSD는 라인업을 주지 않음" 결론을 믿지 말 것 — 코드 버그였음(고정 경로만 탐색). 로그의 `[diag]` 원문이 진실.

---

## 알려진 소소한 관찰 (급하지 않음)
- 특정 실행에서 BSD 502로 한 리그 스쿼드가 일부만 잡힐 수 있음(예: 에레디비시 11/18팀). "선수 0명 클럽" diag가 어느 팀인지 잡아줌. 서버 문제라 다음 실행 자동 복구.
- app_export의 "일정/스쿼드 매칭 안 된 팀명" 경고 다수(250여 개)는 타 리그·컵대회·타 소스 팀명 노이즈로 무해. EPL FC접미사 20팀 포함.
- `data/metrics/*.json`은 StatsBomb 공개데이터(북유럽/친선전 등)라 리그 득점왕엔 못 씀 — 이래서 collect_goalscorers 전용 수집기를 따로 둔 것.
