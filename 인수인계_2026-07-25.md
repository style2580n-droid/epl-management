# epl-management 인수인계 (2026-07-25)

## 프로젝트 개요
- GitHub `epl-management` 저장소. 매일(6시간마다 자동) 8개 리그(EPL + 라리가/분데스리가/세리에A/리그1/에레디비시/챔피언십/**MLS**/**엘리테세리엔**) 데이터 수집 → 예측 앱 2개 공급.
- 앱: `EPL_index.html`(EPL 전용), `multi_league_index.html`(8개 리그 종합).
- 뿌리: 원래 파이프라인 없이 수동 운영하던 `index.html`(월드컵 예측기)에서 파생 → EPL_index.html → 거기서 다시 파생해서 8개 리그 종합앱을 만듦. 세 앱의 UI 성숙도 차이가 큼(아래 "다음 할 일" 참고).
- 메인 소스: BSD(BSDClient). 보조: football-data.org, TheSportsDB, FPL, Odds API.
- 출력: `reports/app_data.js`(EPL), `reports/app_data_multileague.js`(8개 리그).
- GitHub Actions "Football Data Center Pipeline" — **cron 6시간 간격으로 방금 변경함**(`0 3,9,15,21 * * *` UTC = 한국시간 12/18/00/06시). `workflow_dispatch`(수동 실행)도 항상 가능.

---

## 이번 세션 핵심 성과: MLS·엘리테세리엔 완전 통합
0번(EPL A단계)~작년 세션들 이후, 이번 세션에서 **MLS·엘리테세리엔 두 리그를 파이프라인+클라이언트 양쪽 다 실전 검증 완료**로 마무리함. 백엔드→클라이언트 순서로 총 6단계 버그를 실전 로그로 확인하며 수정:

1. **리그/팀 매핑**: `LEAGUE_MATCHERS`/`LEAGUE_TEAM_MAPS`에 mls(30팀)/eliteserien(16팀) 추가. `collect_fixtures_multileague.py`, `app_export_multileague.py`, `collect_xg_bsd.py`, `collect_transfers_bsd.py` 전부 반영. (`collect_logos_multileague.py`는 원래 범용 구조라 손 안 대도 자동으로 됐음 — 확인됨, 재검토 금지)
2. **득점왕 팀 매칭 100% 실패 버그**: `collect_goalscorers.py`가 골 이벤트에서 `team`/`team_name` 필드를 찾는데, 실제 BSD 응답(`events/{eid}/incidents/`)엔 그 필드가 아예 없고 대신 **`is_home`(bool)**으로 옴 — `_goals_from_items()`와 `app_export_multileague.py`의 `build_leaderboard()`를 `is_home` 기반으로 수정. 실전 확인: MLS 785골·엘리테세리엔 316골 전부 팀명 정상.
3. **날짜필터 버그(근본원인)**: `collect_fixtures_multileague.py`의 `_fetch_league_events()`가 `DATE_FROM=오늘-30일` 고정값을 써서, 이미 몇 달째 진행 중인 MLS(1월 개막)/엘리테세리엔(1월 개막)의 과거 경기가 전부 조회 자체가 안 됐음(6개 유럽리그는 개막 전이라 여태 안 드러났었음). `_find_leagues()`가 찾은 리그의 실제 `season.start_date`를 사이드채널(`_LEAGUE_SEASON_START` dict)에 저장해서 리그별로 `date_from`을 다르게 씀 — 외부 소비자(`collect_goalscorers.py`/`collect_lineups.py`)의 3-tuple 언패킹은 안 건드림. 실전 확인: MLS 종료경기 20건→238건, 엘리테세리엔 18건→107건으로 정상화. **이 함수 하나 고쳐서 득점왕/라인업도 같이 좋아짐.**
4. **순위표 미반영**: `collect_fixtures_multileague.py`가 종료경기를 SCHEDULE에서 아예 빼고 있었음(`homeGoals`/`awayGoals` 필드도 없었음) — 클라이언트의 `computeStandingsTable()`/`StandingsTab`은 처음부터 이 데이터를 기다리도록 이미 짜여 있었음(설계자가 미리 만들어뒀는데 데이터가 한 번도 안 왔던 것). 종료경기도 `homeGoals`/`awayGoals`와 함께 포함하도록 수정.
5. **시즌시뮬레이션 부정확**: `multi_league_index.html`의 `simulateSeason()`이 이미 끝난 경기까지 매번 무작위 재시뮬레이션하던 것 수정 — `homeGoals`/`awayGoals` 있는 경기는 실제 결과를 승점에 그대로 반영(1회만 계산), 없는 경기만 몬테카를로 대상으로.
6. **클라이언트 신규 리그 배선**: `LEAGUES`/`LEAGUE_ORDER`/`ENGLISH_NAME`/`LEAGUE_LAMBDA`(footystats.org 2026 실측: MLS 홈1.91/원정1.38, 엘리테세리엔 홈1.85/원정1.13)/`RELEGATION_ZONE`(MLS=0 강등없음, 엘리테세리엔=2). MLS는 ELO 0팀(ClubElo가 미국 미커버, 구조적 한계)이라 xG(30팀 100% 확보)가 대신 신호 역할.

**검증 방법**: 매번 실전 파이프라인 로그(GitHub Actions 스크린샷)로 확인. 로컬에서도 스텁 데이터로 rule-3 종단 테스트 병행.

---

## 이번 세션 추가 작업: EPL과 8개리그 앱 UI 격차 해소 (진행 중, 미완)
사용자가 EPL_index.html(및 그 뿌리인 index.html 월드컵판)과 multi_league_index.html을 비교하며 지적한 격차들:

### 확인 완료된 사실
- **"기록"(History) 탭은 이미 있었음** — 첫 검색에서 "탭이 아예 없다"고 잘못 판단했었는데(이모지로 시작하는 라벨을 못 잡는 grep 패턴 실수), 실제론 `HistoryTab`/예측저장/브라이어스코어/방향적중률/캘리브레이션/리그별삭제 다 있었음. **이 실수를 教訓삼아, 코드에 없다고 결론 내리기 전에 검색 패턴부터 의심할 것.**
- **개별경기 예측(`predictMatchV2`)은 이미 EPL과 동일하게 10만 회 몬테카를로 사용 중.** 문제없음.
- 진짜 차이: **EPL은 브라우저가 직접 외부 API(`fetchSportScore`)로 결과를 긁어오고, 저장 즉시 동적 ELO를 갱신함(`updateEloAfterMatch`/`saveDynamicElo`)** — 이건 원래 파이프라인이 없던 시절 설계라 그런 것. **8개리그 앱은 이미 파이프라인이 실제 스코어를 SCHEDULE에 넣어주니까, 외부 API를 흉내낼 필요 없이 파이프라인 데이터로 자동정산하는 게 더 낫고 실제로 그렇게 구현함(아래).**
- **버그 발견 및 수정**: `TeamStyleRadarCard`의 `hasXg` 체크가 8개 리그 전체를 묶어서 판단하고 있어서, 엘리테세리엔(xG 0팀)인데도 다른 리그에 xG 있다는 이유로 "xG 없음" 경고가 안 뜨던 버그 → 리그별로 다시 체크하도록 수정함.

### 이번 세션에 새로 구현한 것 (multi_league_index.html)
1. **자동정산**: 기존에 저장해둔 예측 중 미정산 건을, 수동 스코어 입력 없이 파이프라인 `SCHEDULE`의 실제 결과와 자동 대조(`useEffect`로 매치되면 자동으로 `settled:true` 처리, `settledBy:"auto"` 태그). 매치 안 되면 기존 수동입력 UI가 폴백으로 그대로 남음.
2. **"자동 기록" 신규 카드**(`AutoRecentResultsCard`/`AutoResultRow`) — 사용자의 핵심 지적("MLS/노르웨이 종료경기 예측스코어를 볼 수 있어야 하는데 안 보인다")에 대응. **예측을 미리 저장 안 해도**, 파이프라인이 이미 갖고 있는 종료경기 전부를 자동으로 나열하고, 지금 시점 모델로 즉석 계산(사후계산, 3000회 몬테카를로)해서 실제결과와 대조 → ✅스코어적중/🔵방향적중/❌미적중 배지 표시. "기록" 탭 상단(`AccuracyPanel`/`ModelVsMarketPanel` 바로 아래)에 삽입 완료.
   - ⚠️ 정직하게 표기해둠: 이건 경기 전 블라인드 예측이 아니라 사후 계산(그 사이 ELO/xG가 이미 그 경기 결과를 반영했을 수 있음)이라고 UI에 명시함 — 과신 방지.
   - 로직(적중 판정 3가지 케이스: 스코어적중/방향적중/미적중) node로 종단 테스트 완료. 중괄호/소괄호 balance 확인 완료(1956/1956, 1801/1801).
   - **브라우저 실사용 테스트는 아직 안 됨** — babel 실제 파싱도 네트워크 막혀서 못 했음(이전 세션과 동일 제약). 다음 세션에서 실제 배포 후 확인 필요.

### 아직 손 안 댄 것 (다음 세션 작업 대상)
- **동적 ELO(dynamic ELO) 피드백 시스템**: EPL엔 있고 8개리그 앱엔 전혀 없음. 실제 결과가 쌓일 때마다 ELO 레이팅 자체를 갱신하는 기능 — 설계부터 새로 해야 함(정적 ELO는 ClubElo/파이프라인이 주는 값이라 건드리면 안 되고, 별도의 "동적 보정치" 레이어를 얹는 방식이 안전할 듯 — 논의 필요).
- **1~3월 EPL 경기 3건 미스터리**: 사용자가 스크린샷으로 보여준 "2026-01-18 브라이튼 vs 본머스" 등 3건은 **서버/파이프라인 데이터가 아니라 사용자 브라우저의 localStorage에 저장된 개인 예측 기록**으로 확인됨(EPL_index.html의 `manualScores`/`history`가 전부 localStorage 기반). 서버 코드로는 확인/삭제 불가 — 사용자가 직접 앱의 "기록" 탭에서 지우거나 브라우저 사이트 데이터 초기화해야 함. **재조사 불필요, 이미 결론 남.**
- EPL_index.html의 나머지 고급 기능들(핸디캡, 마켓분석/배당비교, 주요 예상 스코어라인 Top5, 결장·출전불투명 상세 사유 목록, 선발라인업 ESPN 자동조회, 골드만삭스 모델 비교, 최근5경기 폼 카드) — 스크린샷으로 확인은 했지만 8개리그 앱에 각각 있는지/없는지 하나씩 대조는 아직 다 못 함. 이번 세션엔 "기록" 탭 관련된 것만 집중함.

---

## 이번 세션 추가 확인: EPL 앱도 자체 파이프라인 데이터를 실제로 받고 있음
(인수인계 문서 최초 작성 이후에 추가로 확인된 내용)

- `EPL_index.html`의 `loadPipelineData()`(8660행)가 `https://raw.githubusercontent.com/{owner}/{repo}/{branch}/reports/app_data.js`를 fetch해서 `PIPELINE_ELO`/`PIPELINE_ADVANCED_STATS`/`PIPELINE_RECENT_FORM`/`PIPELINE_SQUADS` 등을 병합함 — 앱 로딩 시 자동 호출됨.
- `app_export.py`(EPL 전용 출력 생성기) 코드로 직접 확인: `build_team_blocks()`가 `data/master/club_elo.json`(ClubElo 실측 ELO)과 `{METRICS_DIR}/season_teams.json`(SQLite 경기결과 집계 — xG/xGA/npxG/PPDA/PSxG/빅찬스 등)에서 **팀별로 실제 다른 값**을 채움. 특정 팀에 데이터 자체가 없을 때만(예: 승격팀이라 ClubElo 이력 없음) 공유 기본값(`default_elo`/`default_adv`)으로 개별 폴백 — 20팀이 전부 똑같이 나오는 구조가 아님. `RECENT_FORM`도 `SELECT * FROM matches WHERE status='FINISHED'...`로 실제 종료경기 기준.
- **결론**: EPL 앱의 ADVANCED_STATS/RECENT_FORM/ELO는 진짜 실측 파이프라인 데이터입니다 — 재검증 불필요.
- `PIPELINE_SPI`/`PIPELINE_OPTA`는 이 파이프라인이 애초에 안 다루는 항목(추출 목록에 없음) — SPI/Opta 관련 트리가 있다면 그건 파이프라인 존재 여부와 무관하게 별개로 처리해야 함.

### ⚠️ 유의사항: 다른 채팅방에서 EPL_index.html을 동시에 건드리고 있을 가능성
이 세션 도중 사용자가 "SPI/Opta 트리 제거" 관련 내용을 실수로 이 채팅방에 붙여넣은 적이 있음 — 그 내용은 **이 대화에서 다룬 적 없는, 다른 채팅방에서 진행 중인 별도 작업**으로 확인됨(EPL_index.html의 SPI/Opta 관련 트리 삭제 작업으로 추정). 즉 **다른 세션에서 EPL_index.html을 병행 수정 중일 수 있음** — 다음 세션 시작 시 사용자에게 "지금 갖고 있는 EPL_index.html이 최신본이 맞는지, 다른 채팅방에서 진행 중인 작업이 있는지" 먼저 확인할 것. 이 인수인계에 첨부된 `EPL_index.html`은 그 SPI/Opta 제거 작업 이전 버전일 수 있음.

## 이번 세션 추가 작업: EPL 개인기여도(playerImpactPct) 죽어있던 기능 살림
- **원인**: `EPL_index.html`의 `TEAM_GROUP_GAMES = {}`, `STATIC_MATCH_STATS = {}`가 **영원히 빈 객체로 하드코딩**돼 있었음 — 이 두 변수에서 데이터를 가져오는 `playerImpactPct`(개인기여도)가 항상 무의미한 값만 반환하던 죽은 기능이었음. `TEAM_GROUP_GAMES`/`STATIC_MATCH_STATS`가 다른 채팅방에서 사용자가 지적한 항목.
- **핵심 발견**: 새로 데이터를 만들 필요가 전혀 없었음 — `app_export.py`의 `build_squads()`가 이미 `*_metrics.json`(경기별 실측 스탯 파일, 매 파이프라인 실행 로그에 계속 찍히던 그 파일들)에서 `_game_record()`로 골/도움/xG/xA/슛/전진패스/SCA·GCA/빅찬스/키패스/태클/회수/드리블 등 `gameRawScore()`가 필요로 하는 지표를 이미 전부 뽑고 있었음 — 단지 "선수별" 구조(`games_by_player`)로만 쌓이고 "팀→경기→선수배열" 구조(`TEAM_GROUP_GAMES`가 필요로 하는 모양)로는 재구성이 안 됐던 것뿐.
- **수정 내용**:
  1. `app_export.py`: `build_squads()`가 같은 루프에서 `team_group_games`(팀→경기파일명→선수레코드배열)도 같이 만들어서 `(squads, team_group_games)` 튜플로 반환하도록 수정. `render_js()`에서 `PIPELINE_TEAM_GROUP_GAMES`로 출력 추가.
  2. `EPL_index.html`: `loadPipelineData()`의 extract 함수에 `PIPELINE_TEAM_GROUP_GAMES` 추가, 병합 로직 추가(`TEAM_GROUP_GAMES[team] = {...기존, ...새값}`), **`__teamShareCache` 캐시도 병합 시 반드시 비우도록 추가**(안 비우면 앱이 먼저 빈 데이터로 한 번 계산해서 캐시해버리면 나중에 진짜 데이터 들어와도 캐시된 옛날 값만 계속 보여줌 — 놓치기 쉬운 포인트라 명시해둠).
- **테스트**: Python 쪽 핵심 로직(단위테스트, 팀→경기→선수 구조 정확성) + node로 실제 `playerImpactPct` 함수 실행 검증(병합 전 0% → 병합 후 손흥민(FW, 2골1도움) 6.00%, 수비수A(DF) 0.80% — FW가 DF보다 높게 정상 산출) 둘 다 통과.
- **8개리그 앱(multi_league_index.html)엔 이 기능(playerImpactPct/aggregateTeamPlayers/getTeamShares 등 전체 ~250줄짜리 채점 엔진)이 처음부터 아예 없음** — EPL은 "죽은 배선"만 고치면 됐지만, 8개리그는 엔진 자체를 통째로 이식해야 해서 훨씬 큰 작업. 아직 안 함 — 다음 세션 결정 필요.

## 검증 완료 (재확인 금지) 핵심 항목 — 기존 + 이번 세션 추가


**모든 항목은 GitHub Actions 파이프라인 실전 로그(#88~#97, 이 대화 중 스크린샷으로 순차 확인)로 검증됨 — 추측이나 로컬 테스트만으로 끝난 게 하나도 없음.** 아래 각 항목은 "무슨 근거로 검증됐는지"까지 같이 적음.

1. 팀 매칭 8개 리그(6개+MLS+엘리테세리엔) 전부 완성 — **근거**: #90~#97 로그에 매번 "mls teams{'league_id': 18} → 응답 30개 중 30개 ID 매칭", "eliteserien ... 16개 중 16개 ID 매칭" 반복 확인.
2. 라리가 BSD 중복 4쌍(아틀레틱 911/51, 아틀레티코 54/920, 에스파뇰 53/919, 레알소시에다드 48/924) — 매 로그에 `[diag]`로 뜨는 정상 현상, 버그 아님.
3. 득점왕 엔드포인트 `events/{eid}/incidents/` 확정, `team`/`team_name` 필드 없고 `is_home`(bool)만 옴 — **근거**: `rehearse_goal_team_probe.py` 실행 로그(#94)에서 원문 아이템 전체값 직접 출력해서 눈으로 확인(`'is_home': True`, team 필드 자체가 없음). 이후 #95~#97에서 "팀 매칭 실패" 문구 완전히 사라진 것까지 재확인.
4. 라인업 엔드포인트 `events/{eid}/lineups/` 확정.
5. player_baseline A단계: EPL·8개리그 클라이언트 완성. **근거**: #97 로그 "선수 기준선 갱신: ... source=league 전환 24명" — 이 파이프라인 사상 최초로 0이 아닌 값 나온 것 확인(그 전까진 게임수 부족으로 0이 정상이었음).
6. collect_lineups.py 재사용 캐싱 정상 — 매 로그에 "(기존 재사용 N건)" 정상 출력.
7. MLS id=18/season_id=158(시즌시작 2026-01-01), 엘리테세리엔 id=54/season_id=1230(시즌시작 2026-01-01) — **근거**: #97 로그 "mls 발견: ... season_start=2026-01-01" 직접 출력 확인.
8. ClubElo: 엘리테세리엔 16팀 전부 커버. MLS(USA) 미커버 — 구조적 영구 공백, xG로 대체 완료. **근거**: #90~#97 로그에 매번 "mls: ... ELO 0팀 ... xG 30팀" vs "eliteserien: ... ELO 1팀 ... xG 0팀" 패턴 일관되게 반복 — MLS는 xG가, 엘리테세리엔은 ELO가 주 신호. 이 비대칭은 의도된 것이지 버그 아님(엘리테세리엔은 xG 대신 ELO로 커버되니까 없어도 됨).
9. `_LEAGUE_SEASON_START` 사이드채널 방식으로 날짜필터 고침 — **근거**: #97 로그에서 MLS 종료경기 20건(#93까지)→238건(#97), 엘리테세리엔 18건→107건으로 정확히 뛴 것 확인. 로컬 스텁 테스트(3월 과거경기 포함 여부)도 병행.
10. `computeStandingsTable`/`StandingsTab`/`simulateSeason` — 클라이언트 로직 자체는 원래도 맞았고(설계자가 미리 만들어뒀던 것), 백엔드가 종료경기+스코어를 안 보내던 게 원인이었음 — **근거**: #96 로그에서 "mls: 경기 294건 중 일정 294건"(이전엔 274건, 20건 빠졌었음)으로 정확히 일치. `simulateSeason` 로직 수정은 node로 실행 검증(A팀 2-0 실제승리 → titleProb 100% 정상 산출).
11. `predictMatchV2`/`computeLambdas` — MLS/엘리테세리엔에서 크래시 안 남, **코드 리뷰**로 확인(xG 없으면 자동 폴백, ELO 없으면 1500 기본값, 부상/피로/선수기준선 전부 데이터 없으면 factor=1 무효과) — ⚠️ 이 항목만 유일하게 **실제 브라우저 실행 검증은 아직 안 됨**, 코드 정적분석 기준. 다음 세션 최우선 확인 대상(이미 "다음 세션 할 일 1번"으로 명시돼 있음).
12. "기록" 탭이 원래부터 있었다는 것 — **근거**: `grep -n "id:\"history\""` 재검색으로 라인 2987 확인, `HistoryTab`/`AccuracyPanel`/`computeAccuracyMetrics` 함수 실체 코드로 확인(제가 처음에 이모지 라벨 놓쳐서 "없다"고 오판했던 것 정정).
13. EPL 앱의 ADVANCED_STATS/RECENT_FORM/ELO가 팀별 실측값인 것 — **근거**: `app_export.py`의 `build_team_blocks()` 코드 직접 확인 — `club_elo.json`/`season_teams.json`에서 팀별로 다른 값을 채우는 실제 로직 확인(공유 기본값은 데이터 없는 팀에만 개별 폴백).

## 하지 말 것 (누적)
- 팀 매칭/별칭/diag 재점검, incidents 재프로브, 라인업 경로 재탐색.
- `lineups.json` 경로 사용(db.py 충돌 — 반드시 `lineups_bsd.json`).
- "playerBaseline이 0명이다"를 버그로 취급(개막 전엔 정상, 게임 쌓이면 자동 전환 — 이미 실증됨).
- BSD 502를 코드 버그로 취급(서버 장애).
- EPL A단계 재구현.
- ClubElo에서 MLS 팀 찾기 시도(구조적으로 없음, 이미 xG로 대체 완료).
- MLS 강등권 관련 로직 의심(RELEGATION_ZONE.mls=0으로 의도적 설정, 강등제 자체가 없는 리그라 정상).
- **grep으로 "기능이 없다" 단정하기 전에 이모지/공백 패턴부터 의심할 것**(이번 세션 실수 교훈).
- EPL_index.html의 localStorage 기반 개인 기록(manualScores/history)을 서버/파이프라인 문제로 오인하지 말 것.

## 보류 (사용자 결정, 먼저 꺼내지 말 것)
- odds-api.io 키 평문 노출 (서버 프록시 이전 "패스" 결정).
- 순수 인명 오역 ("양고기 도나트"류).

---

## 다음 세션 시작 시 즉시 할 일
1. **`multi_league_index.html` 배포 확인** — 이번 세션 마지막 수정(자동정산+자동기록카드)이 실제 브라우저에서 정상 렌더링되는지 확인 필요(node 문법검증/로직테스트만 했고 실브라우저 테스트 못 함).
2. **동적 ELO 피드백 시스템 설계부터** — EPL의 `updateEloAfterMatch`/`saveDynamicElo` 구조 참고해서, 8개리그 구조(정적 ELO는 파이프라인이 주는 것)에 안전하게 얹는 방법 논의.
3. EPL_index.html의 나머지 고급 기능(핸디캡/마켓분석/결장상세/라인업자동조회 등) 8개리그 앱 대조표 마저 작성.

---

## 전달해야 할 파일 (다음 채팅방에 이 markdown과 함께 업로드)
아래 파일들을 다음 채팅방 시작할 때 한 번에 업로드하면 이어서 작업 가능:

| 파일 | 현재 상태 | 비고 |
|---|---|---|
| `multi_league_index.html` | 이번 세션 최종본(자동정산+자동기록카드+hasXg버그수정 포함) | **저장소에 아직 커밋 안 했으면 최우선 커밋 필요** |
| `football_pipeline.yml` | cron 6시간 간격으로 변경된 최종본 | 저장소 커밋 필요 |
| `collect_fixtures_multileague.py` | 종료경기 스코어 포함 + 리그별 시즌시작일 date_from 반영 최종본 | 이미 커밋됐을 가능성 높음(직전 세션에서 실전검증까지 끝남) |
| `app_export_multileague.py` | is_home 기반 득점왕 팀판별 최종본 | 이미 커밋됐을 가능성 높음 |
| `collect_goalscorers.py` | is_home 필드 추출 최종본 | 이미 커밋됐을 가능성 높음 |
| `collect_transfers_bsd.py` | mls/eliteserien LEAGUE_MATCHERS 추가 최종본 | 이미 커밋됐을 가능성 높음 |
| `app_export.py` | EPL 전용 출력 생성기 — 이번 세션엔 안 건드렸지만 ADVANCED_STATS/RECENT_FORM 실측 로직 확인용으로 참고함 | 수정 안 함, 검증 근거 자료로만 사용 |
| `EPL_index.html` | 원본(사용자 업로드), 비교 기준용 — 수정 안 함 | 다음 세션에서 대조 작업 시 필요 |
| `index.html`(월드컵판) | 원본(사용자 업로드), 뿌리 UI 참고용 — 수정 안 함 | 다음 세션에서 대조 작업 시 필요 |

**우선순위**: `multi_league_index.html`, `football_pipeline.yml` 이 둘은 이번 대화에서 마지막으로 수정됐고 아직 저장소 커밋 확인 전이라 다음 채팅방에서도 이 버전 그대로 이어받아야 함. 나머지 4개 파이썬 파일은 이전 파이프라인 로그로 이미 실전 반영 확인됐으니 저장소에 이미 있을 가능성이 높지만, 혹시 몰라 백업 차원에서 같이 넘김.
