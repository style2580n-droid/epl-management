# epl-management 인수인계 (2026-07-25 최종판 — 이 문서 하나로 이어갈 것)

## 📌 이 프로젝트가 뭔가 (새 세션 컨텍스트)
GitHub `epl-management` 저장소의 축구 데이터 파이프라인 + 예측 앱 2개.
- 매일 GitHub Actions(`football_pipeline.yml`)가 BSD 등에서 데이터를 수집해
  `reports/app_data.js`(EPL용), `reports/app_data_multileague.js`(8개리그용)를 생성.
- 앱 2개: `EPL_index.html`(프리미어리그 전용), `multi_league_index.html`(8개리그:
  라리가·분데스리가·세리에A·리그앙·에레디비시·챔피언십·MLS·엘리테세리엔).
  둘 다 GitHub Pages로 배포되며, 저장소엔 이름 없는 `index.html`로 올라가서 사용자가
  구분을 위해 업로드 시 `EPL_index.html`/`8개리그_index.html`로 리네임함.
- 두 앱 다 몬테카를로 시뮬레이션(10만회) + ELO/xG 기반 하이브리드 예측 엔진.
- **지금 실제로 화면 정상 작동 확인됨**(사용자 스크린샷 다수로 이 대화에서 직접 확인) —
  두 앱 다 렌더링/기본 기능은 이상 없음. 이 부분은 재확인 불필요.

---

## ⚠️ 새 세션 필독 — 가장 중요한 것

**이 세션에서 실제로 있었던 일**: 사용자가 "재검증하지 말라"고 문서에 명시했는데도
이미 검증된 것을 계속 다시 의심하고 재확인을 요구해서 **몇 시간이 낭비됨**. 이전에도
파이프라인 처음 만들 때 다른 AI 때문에 **5일을 날린 전례가 있음** — 결론은 항상
"검증"이었음. 사용자는 이 패턴에 지쳐있고 신뢰가 깎인 상태.

**다음 세션이 반드시 지킬 것**:
1. 사용자가 "올렸다/커밋했다/줬다"고 하면 **그 말을 그대로 믿는다.** 스크린샷 증거를
   다시 요구하지 않는다.
2. 확인이 필요하면 사용자에게 재확인을 요청하는 대신, **코드가 스스로 증명하게
   만든다.** (예: `app_export_multileague.py`에 심어둔 로그 마커 — 아래 "0번" 참고.
   팀 매칭 개수로 간접 추론하다가 몇 시간 날린 전례 있음 — 마커 방식이 확실함.)
3. 이 문서에 "완료"로 적힌 항목은 다시 파일을 요청하거나 재검증하지 않는다.
4. 배경 설명 늘어놓지 말고, 짧고 정확하게, 바로 작업한다.
5. 사용자는 **폰으로만 작업/확인함.** UI 작업 시 이 점을 최우선으로 고려할 것
   (스크롤 최소화, 접기/펼치기 적극 활용).

---

## ✅ 이번 세션에 완료된 것 (전부 코드 실행/실전 로그/저장소 대조로 검증됨 — 재검증 금지)

### multi_league_index.html (8개리그 앱)
- 동적 ELO 델타 레이어(`ELO_DELTA`, 정적 ELO는 안 건드리고 위에 더하는 방식) 신규 구현
  — node 단위테스트 12개 통과.
- 핸디캡(`getHandicapProbs`) EPL에서 포팅 + UI카드.
- 고급지표 추정(`getAdvancedStatsEstimate`, ELO/xG 기반, "FBref 실측" 주장 안 하는
  정직한 라벨) + UI카드.
- 최근5경기폼(`getTeamRecentForm`/`RecentFormPanel`) 신규 추가 — 실제 SCHEDULE(파이프라인
  실측 종료경기)만 쓰고 별도 저장소 안 만듦.
- 일정→예측 바로가기(`onSelectMatch`/`pendingMatch`, "▶" 버튼) 신규 추가.
- **버그 수정**: `getTeamRecentForm`/`RecentFormPanel` 중복선언(실제 배포 후 사용자
  스크린샷에서 발견된 SyntaxError, 화면 전체가 안 뜨는 크래시였음) → 제거 완료, 이후
  재배포해서 정상 작동 화면 스크린샷으로 확인함.
- 실제 React SSR(Node, 브라우저 없이 react-dom/server로 렌더링)로 검증 완료,
  `predictMatchV2` 실제 함수 직접 호출 테스트 완료(재구현 아님, 파일에서 그대로 추출).
- **저장소 반영 확인됨**: 사용자가 올린 `8개리그_index.html`을 grep으로 직접 대조
  (`ELO_DELTA` 26건, `getHandicapProbs` 3건, `pendingMatch` 8건 등 전부 존재, 중복선언
  없음 확인).

### EPL_index.html
- `ADVANCED_STATS` 하드코딩(20팀 완전 동일값 — psxg:1.35 등)이었던 것 제거 →
  기존에 있던 ELO/xG 추정 폴백 로직이 정상 작동하게 됨, 라벨도 "FBref/FotMob 기준"
  (사실 아니었음)에서 "ELO·xG 기반 추정치"로 정직화.
  - **단, `psxg`/`psxgAllowed`/`bigChances`/`bigChancesAllowed`는 파이프라인이 실측으로
    채워줄 수 있는 필드**(`impact_engine.py`가 계산 → `season_teams.json` →
    `app_export.py`가 읽음). `shotQuality`와 세트피스/공중볼/GK류는 파이프라인에도
    override 로직이 없어서 여전히 고정값 — 이건 정상(파이썬 쪽 미구현일 뿐 버그 아님).
- `SPI_RATINGS`/`OPTA_RANKINGS`(20팀 완전 동일값, FiveThirtyEight 2023년 서비스종료·
  Opta 개인프로젝트 접근불가로 실제 데이터 확보 불가 확정)가 ML앙상블의 트리 2개에
  쓰이던 것 발견 → 해당 트리 2개 제거, 남은 3트리(ELO/xG/폼)만 사용.
- `decisionTree4`(폼): 죽은 `formArr`(파이썬 쪽도 override 없음, 진짜 죽은 필드) 대신
  실제 `getRollingXgForm`(`_liveResults` 기반, 파이프라인이 실제로 채움) 연결.
  `fieldTiltDiff`는 대체 실측 소스가 없어서 제거.
- `RadarPanel`(전력비교 레이더): 6축 중 4축(공격/수비/폼 일부/스타/세트피스)이 죽은
  필드였던 것 확인 → 실제 득실점평균(`_liveResults`)+실제 롤링폼 기반 4축(공격/수비/
  득점력/최근폼)으로 재설계, 대체 불가능한 스타·세트피스 축은 제거.
- **이 세션 최대 발견**: `train_ml_ensemble.py`가 2026-07-16부터 매일 만들어온 진짜
  학습모델(`PIPELINE_ML_ENSEMBLE`, 로지스틱회귀)을 EPL_index.html이 한 번도 안 읽고
  있었음(주석엔 "가짜 트리를 진짜모델로 교체한다"고 의도는 적혀 있었는데 연결 코드가
  없었음). 8개리그 앱에 이미 있던 연결 로직(`mlXgFeature`/`mlEnsembleProbs`) 그대로
  이식 — 진짜 모델 있으면 20% 가중치로 블렌딩, 없으면 기존 3트리 40%로 안전 폴백.
  `usedRealMl` 플래그로 UI에서 "학습된 머신러닝 모델" vs "보조 예측 규칙" 구분 표시.
- **저장소 반영 확인됨**: 사용자가 올린 `EPL_index.html`을 grep으로 직접 대조
  (`ADVANCED_STATS = {}`, 트리 3개만, `mlEnsembleProbs`/`usedRealMl` 존재,
  `getSeasonGoalsAvg` 존재, 중복선언 없음 전부 확인).

### 파이프라인 파이썬 파일
- **`collectors.py`**: `EloCollector.run(top_n=100)` → **`top_n=2000`**으로 수정.
  원인: ClubElo가 주는 데이터를 "전세계 통합 순위 상위 100위"로 잘라서 저장하고
  있었음 — 리그 내 순위가 아니라 전세계 순위라서, 에레디비시·챔피언십·엘리테세리엔처럼
  리그 자체가 전세계 기준 낮은 곳은 팀 대부분이 잘려나가 기본값(1500)으로 예측되던
  버그. **실전 로그로 효과 확인됨**(ELO 확보팀 51→99팀으로 거의 2배 증가, 라리가
  12→13, 분데스 5→8, 세리에 14→20, 리그앙 12→17, 에레디비시 2→13, 챔피언십 5→15,
  엘리테세리엔 1→13).
- **`app_export_multileague.py`**: `LEAGUE_TEAM_MAPS`에 26-27시즌 현역인데 누락됐던
  16팀 추가 — 지로나·마요르카·레알오비에도(라리가), 상트파울리·볼프스부르크(분데스),
  크레모네세·엘라스베로나·피사(세리에A), 메스·낭트(리그앙), 볼렌담·헤라클레스알멜로·
  NAC브레다(에레디비시), 레스터·옥스퍼드·셰필드웬즈데이(챔피언십). 코번트리/헐/
  입스위치는 **의도적으로 제외**(이미 EPL 로스터로 확정 수집되고 있음 —
  `[collect_coaches]` 로그에 아스날·맨시티와 나란히 20개 팀으로 찍힘. BSD의 xG
  데이터만 작년 챔피언십 소속으로 잘못 태깅된 것으로 보이는 별개 문제라, championship
  매핑에 추가하면 오히려 리그 오귀속 위험).
  - **확인용 로그 마커 심어둠**: 파일 로드 시 다음 줄이 출력됨 —
    `[app_export_multileague] LEAGUE_TEAM_MAPS 로드 완료 — 총 346개 별칭, 2026-07-25
    16팀 추가판(...)`. **다음 파이프라인 로그에서 이 줄이 뜨는지만 보면 반영여부
    100% 확정됨** — 팀 매칭 개수로 간접 추론할 필요 없음.
  - 사용자가 커밋 스크린샷(PR #105, main 브랜치, Queued 상태)을 이미 보여줬음 →
    반영됐을 가능성 높으나, 이 마커가 다음 로그에 실제로 찍히는지가 최종 확인점
    (아래 "0번" 참고 — 이거 확인 전엔 "반영 안 됐다"고 다시 의심하지 말고, 로그 보고
    마커 있으면 확정, 없으면 그때 커밋 여부 문의).
- **`impact_engine.py`**: `big_chances_allowed`(빅찬스 허용) 필드 추가 — 기존
  `PSxG_faced`와 완전히 동일한 패턴으로 상대팀 관점 미러링(`T[teams[t]]
  ['big_chances_allowed'] += 1`). 실제 `compute_match_metrics()` 함수에 합성 경기
  이벤트 넣어서 생성=허용 정확히 1:1 미러링되는 것 검증 완료.
- **`compute_advanced_stats.py`**(이번 세션에 새로 작성): **폐기됨** — 처음엔 BSD
  원시 이벤트로 고급지표를 직접 계산하는 엔진을 새로 만들려 했으나, 이미
  `impact_engine.py`가 훨씬 정교하게(진짜 학습된 xG모델 포함) 구현하고 있는 걸 뒤늦게
  발견함. **이 파일은 이제 안 씀, 되살리지 말 것.**

---

## ▶ 다음 세션이 할 일 (우선순위 순)

### 0. ~~마커 로그 확인~~ ✅ 완료 (2026-07-25 확인됨 — 재검증 금지)
마커 정상 출력 확인됨: `[app_export_multileague] LEAGUE_TEAM_MAPS 로드 완료 — 총
346개 별칭...`(파이프라인 전체에서 반복 출력, 정상). "한글 매칭 안 됨" 목록
20→4개로 감소, 잔여 4개(`1. FC Heidenheim`, `Coventry City`, `Hull City`,
`Ipswich Town`)는 전부 의도된 제외 대상. 신규 16팀(레스터·옥스퍼드·셰필드웬즈데이·
마요르카·레알오비에도·메스·낭트·볼프스부르크 등) 이적감지 로그도 정상 포착 확인.
반영 100% 확정 — 다시 로그 요청하거나 팀 매칭 개수로 재추론하지 말 것.

### 1. ~~🆕 UI 정리 — 접기/펼치기~~ ✅ 완료 (2026-07-25, 일정탭+예측탭 둘 다)
일정탭은 2번과 함께 해결(종료경기 접기). **예측탭도 완료**: `CollapsibleSection`
공용 컴포넌트 신설, 핵심 결과(팀 매치업/확률바/예상 스코어 Top5/저장 버튼)만 항상
펼침, 나머지(예측근거·상대전적·핸디캡·고급지표·레이더·최근폼·라인업·심판성향·
가치베팅·배당비교 등)는 접어서 기본 숨김 + 탭 하면 펼쳐지는 구조로 그룹핑.
  - EPL 앱: 3개 그룹(예측근거&컨텍스트 / 상세통계 / 심화분석패널 / 가치베팅·컨디션,
    총 4개)으로 분리.
  - 8개리그 앱: 3개 그룹(예측근거&상대전적 / 상세통계 / 배당비교·공유)으로 분리.
  - 괄호/중괄호 균형 스크립트로 원본 대비 대조 완료(신규 불일치 없음). babel 직접
    트랜스파일은 네트워크 제한으로 못 돌려봄 — **실제 배포 후 스크린샷으로 최종
    확인 필요**(여기만 재확인 O, 나머지 재검증 X).

### 2. ~~🆕 일정탭 정렬 문제~~ ✅ 완료 (2026-07-25, EPL·8개리그 앱 둘 다 수정)
**원인 재확인**: 정렬 로직 자체는 날짜 오름차순으로 정확했음(버그 아님) — 지난
경기와 다가오는 경기를 구분해서 보여주는 로직이 없어서, 1월 경기부터 8월 경기까지
한 줄로 쭉 나열되는 게 문제였음(사용자 스크린샷으로 8개리그 앱에서도 동일 증상
재확인: 2026-02-22 → 2026-07-26 순으로 몇 달치가 이어짐).
**적용한 수정**(`ScheduleTab`, 두 앱 동일 패턴):
  1. 다가오는 경기를 최상단에 항상 표시 (날짜 오름차순).
  2. 종료된 경기는 하단에 접어서 기본 숨김, 버튼으로 펼치면 최신순(내림차순)으로 표시.
  3. **월별 칩 추가**(사용자 요청 "내가 원하는 달 경기 예측할 수 있게"): 상단에
     "전체 / 26.02 / 26.03 / ..." 가로 스크롤 칩, 탭 하면 그 달 경기만(과거+미래 무관)
     바로 보여줌.
- 코드 검증: 괄호/중괄호 균형 스크립트로 원본 대비 대조 완료(신규 불일치 없음),
  babel 트랜스파일러는 네트워크 제한으로 직접 못 돌려봄 — **사용자가 실제 배포 후
  화면 스크린샷으로 최종 확인 필요**(이 부분만 재확인 요청 O, 나머지 재검증 X).
- 파일: `EPL_index.html`, `multi_league_index.html` 둘 다 수정본 전달함(1,2번 함께
  반영된 최종본).

### 3. 🆕 종료경기 자동집계 — 적중/미적중 + 개인지표/고급지표 계산값
사용자 요청 원문: "종료된 경기 기록은 적중 미적중 개인지표 고급지표에 필요한 계산값
자동으로 모아지게 만들어야 하고".
- **적중/미적중 자동판정**: 8개리그 앱엔 이미 자동정산(`settledBy:"auto"`) +
  `AutoRecentResultsCard`가 있음(이번 세션 이전에 완료됨, 재구현 금지). **EPL 쪽엔
  동등한 자동정산이 있는지 다음 세션이 확인** — `EPL_index.html`에 `AccuracyPanel`
  (브라이어스코어/방향적중률, "기록" 탭)은 있는 걸 이번 세션에 화면으로 확인했지만,
  이게 저장된 예측을 **자동으로** 정산하는지 아니면 수동 입력에 의존하는지는 코드
  레벨로 다시 확인 안 함 — 다음 세션이 확인.
- **개인지표/고급지표 자동 반영**: `impact_engine.py`(경기별 계산) →
  `season_aggregator.py`(시즌 누적, 이번 세션엔 내용 확인 안 함) →
  `app_export.py`/`app_export_multileague.py`(앱으로 export)로 이어지는 파이프라인이
  이미 이 역할을 하고 있음. **다음 세션 할 일은 새로 만드는 게 아니라, 이 흐름이
  종료경기가 생길 때마다 누락 없이 자동으로 돌고 화면(고급지표비교 카드, 시즌기록탭
  등)에 반영되는지 점검·정리하는 것.**
- `season_aggregator.py`는 이번 세션에 코드를 받아본 적 없음 — 필요하면 다음 세션이
  요청.

---

## 🚫 하지 말 것 (누적)
- 이번 세션에 "완료"로 적힌 항목 재검증 요청 (multi_league/EPL 두 앱의 위 기능들,
  collectors.py, impact_engine.py, app_export_multileague.py 마커/16팀 반영,
  일정탭 정렬/월별칩, 예측탭 CollapsibleSection 접기).
- 사용자가 "올렸다/커밋했다"고 하면 스크린샷 증거 다시 요구하기.
- ADVANCED_STATS를 "전부 가짜"라고 단정 — psxg/psxgAllowed/bigChances/
  bigChancesAllowed는 파이프라인이 실측으로 채울 수 있음.
- 팀 매칭/별칭/diag 재점검, incidents 재프로브, 라인업 경로 재탐색.
- `lineups.json` 경로 사용(반드시 `lineups_bsd.json`).
- 코번트리/헐/입스위치를 championship 매핑에 추가(EPL 소속 확정됨, 추가하면 오귀속).
- `compute_advanced_stats.py` 되살리기(impact_engine.py로 대체됨, 폐기된 파일).
- "1~3월 EPL 경기 3건 미스터리" 재조사(이미 결론 남 — localStorage 개인기록).
- "ML 학습 표본 부족으로 스킵"을 문제로 취급(150건 미만이면 정상 스킵, 기존 모델 유지).

## ⏸ 보류 (사용자 결정 대기 — 먼저 꺼내지 말 것)
- odds-api.io 키가 `multi_league_index.html`에 평문 노출 — 사용자가 "패스" 결정.
- 순수 인명 오역(음역 시스템 필요, 근본 미해결).
